import { LeadsPage } from "./LeadsPage"

const NoAnswer = () => {
  return (
    <LeadsPage
      remarkFilter="No Answer"
      title="No Answer Leads"
      description="Follow up with leads that haven't responded to initial contact attempts."
    />
  );
};

export default NoAnswer;