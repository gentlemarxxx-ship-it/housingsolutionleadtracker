import { LeadsPage } from "./LeadsPage"

const Index = () => {
  return (
    <LeadsPage
      remarkFilter="Leads"
      title="Leads"
      description="Manage all active leads that are currently in the pipeline."
    />
  );
};

export default Index;